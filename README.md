# 📚 Моя Библиотека - Веб-приложение для управления книгами

Современное веб-приложение для управления личной библиотекой книг с использованием MongoDB.

## 🎯 Описание проекта

**Моя Библиотека** - это полнофункциональное веб-приложение, позволяющее вам:
- ✨ Добавлять книги с подробной информацией
- 📖 Просматривать коллекцию в красивом интерфейсе
- 🔍 Фильтровать по жанрам и статусу прочтения
- ⭐ Оценивать книги и добавлять заметки
- ✏️ Редактировать и удалять записи
- 📊 Просматривать статистику вашей библиотеки

## 🛠️ Технологии

### Backend
- **Node.js** - серверная платформа
- **Express.js** - веб-фреймворк
- **MongoDB** - нереляционная база данных
- **Mongoose** - ODM для работы с MongoDB

### Frontend
- **HTML5** - структура
- **CSS3** - современный дизайн с градиентами и анимациями
- **JavaScript (ES6+)** - интерактивность

## 📊 Структура данных в MongoDB

Каждая книга в базе данных содержит следующие поля:

```javascript
{
  title: String,          // Название книги (обязательное)
  author: String,         // Автор (обязательное)
  genre: String,          // Жанр (обязательное)
  year: Number,           // Год издания
  description: String,    // Описание книги
  isRead: Boolean,        // Прочитана или нет
  rating: Number,         // Рейтинг от 0 до 5
  notes: String,          // Личные заметки
  coverUrl: String,       // URL обложки книги
  dateAdded: Date         // Дата добавления
}
```

### Доступные жанры:
- Фантастика
- Детектив
- Роман
- Классика
- Научпоп
- Фэнтези
- Триллер
- Биография
- История
- Другое

## 🚀 Установка и запуск

### Предварительные требования

1. **Node.js** (версия 14 или выше)
   - Скачайте с [nodejs.org](https://nodejs.org/)

2. **MongoDB** (версия 4.4 или выше)
   - **Вариант 1:** Локальная установка с [mongodb.com](https://www.mongodb.com/try/download/community)
   - **Вариант 2:** Облачная база данных [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)

### Шаги установки

1. **Клонируйте или скачайте проект**
   ```bash
   cd BDMongo
   ```

2. **Установите зависимости**
   ```bash
   npm install
   ```

3. **Настройте переменные окружения**
   
   Создайте файл `.env` в корне проекта:
   ```env
   MONGODB_URI=mongodb://localhost:27017/bookLibrary
   PORT=3000
   ```

   **Для MongoDB Atlas** используйте connection string:
   ```env
   MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bookLibrary
   PORT=3000
   ```

4. **Запустите MongoDB** (если используете локальную установку)
   ```bash
   mongod
   ```

5. **Запустите приложение**
   
   **Режим разработки** (с автоперезагрузкой):
   ```bash
   npm run dev
   ```

   **Обычный запуск**:
   ```bash
   npm start
   ```

6. **Откройте браузер**
   ```
   http://localhost:3000
   ```

## 📁 Структура проекта

```
BDMongo/
├── public/              # Фронтенд файлы
│   ├── index.html      # Главная страница
│   ├── styles.css      # Стили приложения
│   └── app.js          # Клиентская логика
├── server.js           # Express сервер и API
├── package.json        # Зависимости проекта
├── .env               # Переменные окружения (создать самостоятельно)
├── .gitignore         # Игнорируемые файлы
└── README.md          # Документация
```

## 🔌 API Endpoints

### Книги

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/api/books` | Получить все книги (с фильтрами) |
| `GET` | `/api/books/:id` | Получить книгу по ID |
| `POST` | `/api/books` | Создать новую книгу |
| `PUT` | `/api/books/:id` | Обновить книгу |
| `DELETE` | `/api/books/:id` | Удалить книгу |

### Статистика

| Метод | Endpoint | Описание |
|-------|----------|----------|
| `GET` | `/api/stats` | Получить статистику библиотеки |

### Параметры фильтрации

При запросе `GET /api/books` можно использовать параметры:
- `genre` - фильтр по жанру
- `isRead` - фильтр по статусу прочтения (true/false)
- `sortBy` - сортировка (title, author, year, rating, dateAdded)

Пример:
```
GET /api/books?genre=Фантастика&isRead=true&sortBy=rating
```

## 💡 Примеры использования

### Добавление книги через API (curl)

```bash
curl -X POST http://localhost:3000/api/books \
  -H "Content-Type: application/json" \
  -d '{
    "title": "1984",
    "author": "Джордж Оруэлл",
    "genre": "Классика",
    "year": 1949,
    "description": "Антиутопический роман",
    "isRead": true,
    "rating": 5
  }'
```

### Получение всех книг

```bash
curl http://localhost:3000/api/books
```

### Получение статистики

```bash
curl http://localhost:3000/api/stats
```

## ✨ Основные возможности интерфейса

### 📊 Панель статистики
- Общее количество книг
- Количество прочитанных книг
- Количество книг к прочтению
- Средний рейтинг всех книг

### 🔍 Фильтрация и сортировка
- Фильтр по жанру
- Фильтр по статусу прочтения
- Сортировка по различным критериям

### 📖 Карточки книг
- Красивое отображение обложек
- Информация об авторе и жанре
- Статус прочтения с визуальными индикаторами
- Рейтинг в виде звёзд
- Краткое описание

### ➕ Добавление/редактирование книг
- Удобная модальная форма
- Валидация полей
- Интерактивный выбор рейтинга
- Возможность добавить URL обложки

### 👁️ Просмотр деталей книги
- Полная информация о книге
- Увеличенная обложка
- Все заметки и описания
- Быстрый доступ к редактированию

## 🎨 Дизайн

Приложение использует современный Material Design с:
- 🎨 Градиентными заголовками
- 🌈 Цветными индикаторами статуса
- ✨ Плавными анимациями
- 📱 Адаптивным дизайном для мобильных устройств
- 🎭 Красивыми модальными окнами

## 🔧 Настройка MongoDB

### Локальная установка

1. Установите MongoDB Community Server
2. Запустите сервис MongoDB
3. База данных `bookLibrary` будет создана автоматически

### MongoDB Atlas (облако)

1. Создайте бесплатный кластер на [MongoDB Atlas](https://www.mongodb.com/cloud/atlas)
2. Создайте пользователя базы данных
3. Настройте Network Access (добавьте свой IP)
4. Получите connection string
5. Замените в `.env`:
   ```env
   MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/bookLibrary
   ```

## 🐛 Решение проблем

### MongoDB не подключается

**Проблема:** `Ошибка подключения к MongoDB`

**Решение:**
- Убедитесь, что MongoDB запущен: `mongod`
- Проверьте правильность connection string в `.env`
- Для Atlas: проверьте Network Access и пароль пользователя

### Порт уже используется

**Проблема:** `Error: listen EADDRINUSE: address already in use :::3000`

**Решение:**
- Измените порт в `.env`:
  ```env
  PORT=3001
  ```

### Не устанавливаются зависимости

**Проблема:** Ошибки при `npm install`

**Решение:**
- Обновите Node.js до последней версии
- Очистите кэш npm: `npm cache clean --force`
- Удалите `node_modules` и повторите установку

## 📝 Лицензия

MIT License - свободно используйте для личных и коммерческих проектов

## 👨‍💻 Разработка

Проект создан как демонстрация работы с:
- RESTful API
- MongoDB и Mongoose
- Современным фронтенд дизайном
- CRUD операциями
- Клиент-серверной архитектурой

---

**Приятного использования! 📚✨**

