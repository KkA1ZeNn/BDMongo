
|![](Aspose.Words.8ac9c90d-2492-4930-9d3d-4fa56a8a2919.001.jpeg)|<p>**Министерство науки и высшего образования Российской Федерации**</p><p>**Федеральное государственное автономного образовательное учреждение** </p><p>**высшего образования**</p><p>**«Московский государственный технический университет**</p><p>**имени Н.Э. Баумана**</p><p>**(национальный исследовательский университет)»**</p><p>**(МГТУ им. Н.Э. Баумана)**</p>|
| :- | :-: |


|ФАКУЛЬТЕТ|[Ваш факультет]|
| :- | :- |
|КАФЕДРА|[Ваша кафедра]|


**КУРСОВАЯ РАБОТА**

`                              `***ПО ДИСЦИПЛИНЕ:***

|***Проектирование распределённых баз данных***|
| :-: |
|||
|||

***НА ТЕМУ:***

|***Проектирование базы данных*** |
| :-: |
|***веб-приложения "Моя Библиотека"***|
|||


|Студент|`       `[Группа]||||`    `[Фамилия И.О.]|
| :- | :- | :- | :- | :- | :- |
||(группа)||(подпись, дата)||(И.О. Фамилия)|
|Руководитель курсового проекта|||||<p></p><p>[Фамилия И.О.]</p>|
||||(подпись, дата)||(И.О. Фамилия)|


`  `*2024 г*


**Содержание**

[Введение](#введение)

[1. Определение предметной области](#определение-предметной-области)

[2. Объекты таблицы](#объекты-таблицы)

[3. Создание коллекций](#создание-коллекций)

[4. Запросы](#запросы)

[5. Технологии доступа к БД](#технологии-доступа-к-бд)

[Заключение](#заключение)

[Список использованных источников](#список-использованных-источников)


## **Введение**

Современные информационные системы требуют гибких и масштабируемых решений для хранения и обработки данных. В то время как традиционные реляционные СУБД, такие как PostgreSQL и MySQL, остаются надёжным инструментом для работы со структурированными данными, рост требований к производительности, горизонтальному масштабированию и динамическому изменению структуры данных стимулирует переход к нереляционным решениям. Одним из наиболее распространённых представителей класса NoSQL-баз данных является MongoDB, использующая документно-ориентированную модель хранения.

В рамках данной работы рассматривается процесс проектирования и реализации базы данных для веб-приложения "Моя Библиотека" с использованием MongoDB и нативного драйвера Node.js. Проект представляет собой систему управления личной библиотекой книг, позволяющую пользователям добавлять, просматривать, оценивать и организовывать свою коллекцию книг.

Предметом исследования является база данных веб-приложения для управления личной библиотекой. Такая система использует набор сущностей: книги с полной информацией (название, автор, жанр, год издания, рейтинг), механизмы фильтрации и сортировки, статистический анализ коллекции. Применение документно-ориентированного хранения данных позволяет оптимизировать процессы взаимодействия пользователей с системой, ускорить выборки и обеспечить адаптивность структуры данных при расширении функциональности.

Целью работы является проектирование структуры базы данных, описание её компонентов, реализация коллекций MongoDB с использованием нативного драйвера, формирование основных и аналитических запросов, а также определение технологий доступа к базе данных. Особое внимание уделяется методам валидации данных на уровне приложения при отсутствии встроенных механизмов, характерных для реляционных СУБД с ORM.

Результатом работы является документированная структура базы данных, ориентированная на применение в современных веб-приложениях, работающая напрямую с MongoDB без использования промежуточных ODM-библиотек типа Mongoose.


## **1. Определение предметной области**

Предметная область разрабатываемой базы данных охватывает функциональность веб-приложения для управления личной библиотекой книг. Основная задача такой системы — обеспечение удобного взаимодействия пользователя с цифровой коллекцией книг, что включает добавление, просмотр, оценку, фильтрацию и управление библиотекой. Для корректной работы приложения требуется хранение и обработка данных о книгах, включая библиографическую информацию, пользовательские оценки и заметки.

Центральным элементом предметной области являются **книги**, каждая из которых характеризуется набором описательных атрибутов: названием, автором, жанром, годом издания, описанием, статусом прочтения и пользовательским рейтингом. Книги составляют основной контент системы и являются объектом всех операций.

Немаловажную роль играют **жанры**, формирующие классификацию контента и обеспечивающие удобную навигацию. Система поддерживает фиксированный набор жанров: Фантастика, Детектив, Роман, Классика, Научпоп, Фэнтези, Триллер, Биография, История и Другое.

Важным компонентом является механизм **оценки книг**, позволяющий пользователю ставить рейтинг от 0 до 5 и добавлять личные заметки о прочитанном. Эти данные используются для формирования статистики библиотеки и персонализации опыта использования.

Дополнительное значение имеет **статус прочтения** книг, который позволяет разделять коллекцию на прочитанные и запланированные к прочтению книги. Этот механизм обеспечивает организацию библиотеки и отслеживание прогресса чтения.

Система также предоставляет функциональность **фильтрации и сортировки**, позволяющую быстро находить книги по жанру, статусу прочтения, сортировать по названию, автору, году издания или рейтингу. Это обеспечивает эффективную навигацию даже в больших коллекциях.

**Статистический модуль** предоставляет аналитическую информацию о библиотеке: общее количество книг, количество прочитанных и непрочитанных книг, средний рейтинг коллекции. Эти данные формируются через агрегационные запросы MongoDB.

Таким образом, предметная область охватывает комплекс взаимосвязанных сущностей, работа с которыми требует надёжной, гибкой и производительной системы хранения данных. В условиях необходимости быстрого доступа к данным и возможности расширения функциональности применение NoSQL-подхода с документно-ориентированной организацией коллекций позволяет оптимизировать хранение данных и повысить эффективность работы системы.

**Основные объекты предметной области:**

1. **Books (Книги)** — центральная сущность системы
2. **Genres (Жанры)** — классификатор книг
3. **Statistics (Статистика)** — аналитические данные библиотеки


## **2. Объекты таблицы**

### **Описание сущностей и атрибутов базы данных**

#### **Books (Книги)**

Коллекция *books* содержит информацию о книгах в библиотеке. Каждая книга имеет уникальный идентификатор и набор атрибутов, необходимых для работы системы.

**Атрибуты:**

- **\_id (ObjectId)** — уникальный идентификатор книги, генерируется MongoDB автоматически
- **title (String, NOT NULL)** — название книги (обязательное поле)
- **author (String, NOT NULL)** — автор книги (обязательное поле)
- **genre (String, NOT NULL)** — жанр книги из фиксированного списка (обязательное поле)
- **year (Number)** — год издания книги
- **description (String)** — описание или краткое содержание книги
- **isRead (Boolean)** — статус прочтения (true = прочитана, false = не прочитана)
- **rating (Number)** — пользовательский рейтинг от 0 до 5
- **notes (String)** — личные заметки пользователя о книге
- **coverUrl (String)** — URL-адрес обложки книги
- **dateAdded (Date)** — дата и время добавления книги в библиотеку

Коллекция Books формирует основу всей системы и используется во всех операциях приложения.

#### **Genres (Жанры)**

Жанры в данной системе представлены фиксированным перечнем значений и не хранятся в отдельной коллекции, а используются как ограничение на уровне валидации приложения.

**Допустимые значения:**

1. Фантастика
2. Детектив
3. Роман
4. Классика
5. Научпоп
6. Фэнтези
7. Триллер
8. Биография
9. История
10. Другое

Такой подход упрощает структуру базы данных и обеспечивает быструю валидацию данных на уровне приложения.

### **Структура документа книги**

Пример документа в коллекции books:

```javascript
{
  "_id": ObjectId("507f1f77bcf86cd799439011"),
  "title": "1984",
  "author": "Джордж Оруэлл",
  "genre": "Классика",
  "year": 1949,
  "description": "Антиутопический роман о тоталитарном обществе",
  "isRead": true,
  "rating": 5,
  "notes": "Шедевр антиутопии. Актуально и сегодня.",
  "coverUrl": "https://example.com/covers/1984.jpg",
  "dateAdded": ISODate("2024-12-10T10:30:00.000Z")
}
```

### **Ограничения коллекции Books**

**1. Обязательные поля:**
   - title — не может быть пустым, минимальная длина 1 символ
   - author — не может быть пустым, минимальная длина 1 символ
   - genre — должен быть одним из допустимых значений

**2. Валидация типов данных:**
   - title, author, genre, description, notes, coverUrl — строковые типы
   - year — целое число
   - rating — число с плавающей точкой
   - isRead — логический тип (Boolean)
   - dateAdded — тип Date

**3. Диапазоны значений:**
   - year — от 1000 до текущего года
   - rating — от 0 до 5
   - title — максимальная длина 200 символов
   - description — максимальная длина 2000 символов
   - notes — максимальная длина 2000 символов

**4. Ограничения жанра:**
   - genre должен входить в список: ['Фантастика', 'Детектив', 'Роман', 'Классика', 'Научпоп', 'Фэнтези', 'Триллер', 'Биография', 'История', 'Другое']

**5. Значения по умолчанию:**
   - isRead — false (если не указано)
   - rating — 0 (если не указано)
   - dateAdded — текущая дата и время (Date.now())

**6. Автоматическая обработка данных:**
   - Все строковые поля обрабатываются методом trim() для удаления лишних пробелов
   - Даты автоматически преобразуются в объекты Date
   - Числовые значения проходят валидацию перед сохранением

### **Индексы для оптимизации запросов**

Для ускорения операций поиска и сортировки создаются следующие индексы:

```javascript
// Индекс по названию книги
db.books.createIndex({ title: 1 })

// Индекс по автору
db.books.createIndex({ author: 1 })

// Индекс по жанру
db.books.createIndex({ genre: 1 })

// Индекс по дате добавления (для сортировки по умолчанию)
db.books.createIndex({ dateAdded: -1 })
```

### **Отсутствие избыточности данных**

В отличие от реляционных баз данных с нормализацией, данная структура использует денормализованный подход, характерный для MongoDB:

- Все данные о книге хранятся в одном документе
- Нет необходимости в JOIN-операциях
- Быстрый доступ ко всей информации о книге одним запросом
- Упрощённая структура без связанных таблиц

Это обеспечивает высокую производительность операций чтения, что критично для веб-приложения с частым доступом к данным о книгах.


## **3. Создание коллекций**

В отличие от реляционных СУБД, MongoDB не требует явного создания коллекций перед добавлением данных — коллекция создаётся автоматически при вставке первого документа. Однако для обеспечения целостности данных рекомендуется настроить валидацию на уровне MongoDB с использованием JSON Schema.

В данном проекте используется **прямое взаимодействие с MongoDB** через нативный драйвер Node.js без применения ODM-библиотек типа Mongoose. Валидация данных реализована на уровне приложения.

### **Подключение к MongoDB**

```javascript
const { MongoClient, ObjectId } = require('mongodb');

const MONGODB_URI = 'mongodb://localhost:27017';
const DB_NAME = 'bookLibrary';

const client = new MongoClient(MONGODB_URI);

// Подключение к базе данных
async function connectToMongoDB() {
  await client.connect();
  console.log('✅ Подключено к MongoDB');
  
  const db = client.db(DB_NAME);
  const booksCollection = db.collection('books');
  
  // Создание индексов для оптимизации
  await booksCollection.createIndex({ title: 1 });
  await booksCollection.createIndex({ author: 1 });
  await booksCollection.createIndex({ genre: 1 });
  await booksCollection.createIndex({ dateAdded: -1 });
  
  console.log('📇 Индексы созданы');
  
  return { db, booksCollection };
}
```

### **Валидация данных на уровне приложения**

Поскольку используется нативный драйвер без Mongoose, валидация выполняется вручную перед операциями с базой данных:

```javascript
const VALID_GENRES = [
  'Фантастика', 'Детектив', 'Роман', 'Классика', 
  'Научпоп', 'Фэнтези', 'Триллер', 'Биография', 
  'История', 'Другое'
];

function validateBook(bookData, isUpdate = false) {
  const errors = [];

  // Обязательные поля (только при создании)
  if (!isUpdate) {
    if (!bookData.title?.trim()) {
      errors.push('Поле "title" обязательно');
    }
    if (!bookData.author?.trim()) {
      errors.push('Поле "author" обязательно');
    }
    if (!bookData.genre) {
      errors.push('Поле "genre" обязательно');
    }
  }

  // Валидация жанра
  if (bookData.genre && !VALID_GENRES.includes(bookData.genre)) {
    errors.push(`Жанр должен быть одним из: ${VALID_GENRES.join(', ')}`);
  }

  // Валидация года
  if (bookData.year !== undefined && bookData.year !== '') {
    const year = parseInt(bookData.year);
    const currentYear = new Date().getFullYear();
    if (isNaN(year) || year < 1000 || year > currentYear) {
      errors.push(`Год должен быть между 1000 и ${currentYear}`);
    }
  }

  // Валидация рейтинга
  if (bookData.rating !== undefined) {
    const rating = parseFloat(bookData.rating);
    if (isNaN(rating) || rating < 0 || rating > 5) {
      errors.push('Рейтинг должен быть между 0 и 5');
    }
  }

  return errors;
}
```

### **Подготовка данных перед сохранением**

```javascript
function prepareBookData(bookData, isUpdate = false) {
  const prepared = {};

  // Строковые поля (с trim)
  const stringFields = ['title', 'author', 'description', 'notes', 'coverUrl'];
  stringFields.forEach(field => {
    if (bookData[field] !== undefined) {
      prepared[field] = bookData[field].trim();
    }
  });

  // Жанр (без trim, так как из выпадающего списка)
  if (bookData.genre !== undefined) {
    prepared.genre = bookData.genre;
  }

  // Числовые поля
  if (bookData.year !== undefined && bookData.year !== '') {
    prepared.year = parseInt(bookData.year);
  }
  if (bookData.rating !== undefined) {
    prepared.rating = parseFloat(bookData.rating) || 0;
  }

  // Boolean поля
  if (bookData.isRead !== undefined) {
    prepared.isRead = Boolean(bookData.isRead);
  } else if (!isUpdate) {
    prepared.isRead = false;
  }

  // Дата добавления (только для новых документов)
  if (!isUpdate) {
    prepared.dateAdded = new Date();
  }

  return prepared;
}
```

### **Альтернативный подход: JSON Schema на уровне MongoDB**

Хотя в проекте используется валидация на уровне приложения, MongoDB также поддерживает валидацию через JSON Schema:

```javascript
db.createCollection("books", {
  validator: {
    $jsonSchema: {
      bsonType: "object",
      required: ["title", "author", "genre"],
      properties: {
        title: { 
          bsonType: "string", 
          minLength: 1, 
          maxLength: 200,
          description: "Название книги - обязательное поле"
        },
        author: { 
          bsonType: "string", 
          minLength: 1, 
          maxLength: 100,
          description: "Автор книги - обязательное поле"
        },
        genre: { 
          bsonType: "string",
          enum: ['Фантастика', 'Детектив', 'Роман', 'Классика', 
                 'Научпоп', 'Фэнтези', 'Триллер', 'Биография', 
                 'История', 'Другое'],
          description: "Жанр из фиксированного списка"
        },
        year: { 
          bsonType: "int", 
          minimum: 1000, 
          maximum: 2025,
          description: "Год издания"
        },
        description: { 
          bsonType: "string",
          maxLength: 2000,
          description: "Описание книги"
        },
        isRead: { 
          bsonType: "bool",
          description: "Статус прочтения"
        },
        rating: { 
          bsonType: "number", 
          minimum: 0, 
          maximum: 5,
          description: "Рейтинг от 0 до 5"
        },
        notes: { 
          bsonType: "string",
          maxLength: 2000,
          description: "Личные заметки"
        },
        coverUrl: { 
          bsonType: "string",
          description: "URL обложки книги"
        },
        dateAdded: { 
          bsonType: "date",
          description: "Дата добавления в библиотеку"
        }
      }
    }
  }
});
```

### **Создание индексов**

```javascript
// Индекс для быстрого поиска по названию
db.books.createIndex({ title: 1 });

// Индекс для поиска по автору
db.books.createIndex({ author: 1 });

// Индекс для фильтрации по жанру
db.books.createIndex({ genre: 1 });

// Индекс для сортировки по дате добавления (по умолчанию)
db.books.createIndex({ dateAdded: -1 });

// Составной индекс для комбинированных запросов
db.books.createIndex({ genre: 1, rating: -1 });

// Индекс для полнотекстового поиска (опционально)
db.books.createIndex({ 
  title: "text", 
  author: "text", 
  description: "text" 
});
```

### **Graceful Shutdown**

Обеспечение корректного закрытия соединения при остановке сервера:

```javascript
process.on('SIGINT', async () => {
  console.log('\n⏸️  Закрытие соединения с MongoDB...');
  await client.close();
  console.log('✅ Соединение закрыто');
  process.exit(0);
});
```

### **Структура проекта**

```
BDMongo/
├── server.js              # Основной файл сервера с логикой
├── public/                # Статические файлы фронтенда
│   ├── index.html        # HTML интерфейс
│   ├── styles.css        # Стили
│   └── app.js            # Клиентский JavaScript
├── package.json          # Зависимости проекта
├── .env                  # Переменные окружения
└── README.md             # Документация
```

Данный подход обеспечивает гибкость в управлении данными, прямой контроль над MongoDB-операциями и высокую производительность без накладных расходов ODM-библиотек.


## **4. Запросы**

Для обеспечения корректного взаимодействия с базой данных MongoDB были разработаны наборы запросов, позволяющие выполнять операции выборки, добавления, обновления и удаления данных с использованием нативного драйвера Node.js.

### **4.1. Добавление данных (CREATE / INSERT)**

#### **1. Добавление новой книги**

```javascript
// POST /api/books
async function createBook(bookData) {
  // Валидация
  const errors = validateBook(bookData);
  if (errors.length > 0) {
    throw new Error(`Ошибка валидации: ${errors.join(', ')}`);
  }

  // Подготовка данных
  const preparedData = prepareBookData(bookData);

  // Вставка в БД
  const result = await booksCollection.insertOne(preparedData);

  // Получение созданного документа
  const newBook = await booksCollection.findOne({
    _id: result.insertedId
  });

  return newBook;
}
```

**Пример данных для вставки:**

```javascript
{
  title: "1984",
  author: "Джордж Оруэлл",
  genre: "Классика",
  year: 1949,
  description: "Антиутопический роман о тоталитарном обществе",
  isRead: true,
  rating: 5,
  notes: "Шедевр антиутопии",
  coverUrl: "https://example.com/covers/1984.jpg"
}
```

**MongoDB команда:**

```javascript
db.books.insertOne({
  title: "Мастер и Маргарита",
  author: "Михаил Булгаков",
  genre: "Классика",
  year: 1967,
  description: "Философский роман",
  isRead: false,
  rating: 0,
  notes: "",
  coverUrl: "",
  dateAdded: new Date()
})
```

#### **2. Добавление нескольких книг**

```javascript
db.books.insertMany([
  {
    title: "Война и мир",
    author: "Лев Толстой",
    genre: "Классика",
    year: 1869,
    isRead: false,
    rating: 0,
    dateAdded: new Date()
  },
  {
    title: "Преступление и наказание",
    author: "Фёдор Достоевский",
    genre: "Классика",
    year: 1866,
    isRead: true,
    rating: 5,
    dateAdded: new Date()
  }
])
```

### **4.2. Получение данных (READ / SELECT)**

#### **1. Получить все книги**

```javascript
// GET /api/books
const books = await booksCollection
  .find()
  .sort({ dateAdded: -1 })
  .toArray();
```

#### **2. Получить книгу по ID**

```javascript
// GET /api/books/:id
const book = await booksCollection.findOne({
  _id: new ObjectId(bookId)
});
```

**Важно:** В MongoDB необходимо преобразовывать строковый ID в ObjectId:

```javascript
const { ObjectId } = require('mongodb');

// Проверка валидности ID
if (!ObjectId.isValid(bookId)) {
  throw new Error('Неверный формат ID');
}

const book = await booksCollection.findOne({
  _id: new ObjectId(bookId)
});
```

#### **3. Фильтрация по жанру**

```javascript
// GET /api/books?genre=Фантастика
const books = await booksCollection
  .find({ genre: "Фантастика" })
  .toArray();
```

#### **4. Фильтрация по статусу прочтения**

```javascript
// GET /api/books?isRead=true
const readBooks = await booksCollection
  .find({ isRead: true })
  .toArray();

// Непрочитанные книги
const unreadBooks = await booksCollection
  .find({ isRead: false })
  .toArray();
```

#### **5. Комбинированная фильтрация**

```javascript
// GET /api/books?genre=Фантастика&isRead=true
const books = await booksCollection
  .find({
    genre: "Фантастика",
    isRead: true
  })
  .sort({ rating: -1 })
  .toArray();
```

#### **6. Сортировка**

```javascript
// Сортировка по названию (А-Я)
const sortOptions = {
  title: { title: 1 },
  author: { author: 1 },
  year: { year: -1 },
  rating: { rating: -1 },
  default: { dateAdded: -1 }
};

const books = await booksCollection
  .find(query)
  .sort(sortOptions[sortBy] || sortOptions.default)
  .toArray();
```

#### **7. Поиск по тексту (регулярное выражение)**

```javascript
// Поиск книги по части названия
const books = await booksCollection
  .find({
    title: { $regex: /оруэлл/i }  // i = без учета регистра
  })
  .toArray();

// Поиск по названию или автору
const books = await booksCollection
  .find({
    $or: [
      { title: { $regex: /война/i } },
      { author: { $regex: /толстой/i } }
    ]
  })
  .toArray();
```

#### **8. Фильтрация по рейтингу**

```javascript
// Книги с рейтингом >= 4
const highRatedBooks = await booksCollection
  .find({
    rating: { $gte: 4 }
  })
  .sort({ rating: -1 })
  .toArray();
```

#### **9. Фильтрация по году издания**

```javascript
// Книги последних 10 лет
const recentBooks = await booksCollection
  .find({
    year: { $gte: 2014 }
  })
  .sort({ year: -1 })
  .toArray();

// Книги определённого периода
const classicBooks = await booksCollection
  .find({
    year: { $gte: 1800, $lte: 1900 }
  })
  .toArray();
```

### **4.3. Обновление данных (UPDATE)**

#### **1. Обновить книгу**

```javascript
// PUT /api/books/:id
async function updateBook(bookId, updateData) {
  // Валидация
  const errors = validateBook(updateData, true);
  if (errors.length > 0) {
    throw new Error(`Ошибка валидации: ${errors.join(', ')}`);
  }

  // Подготовка данных
  const preparedData = prepareBookData(updateData, true);

  // Обновление в БД
  const result = await booksCollection.findOneAndUpdate(
    { _id: new ObjectId(bookId) },
    { $set: preparedData },
    { returnDocument: 'after' }
  );

  return result.value;
}
```

#### **2. Обновить рейтинг книги**

```javascript
db.books.updateOne(
  { _id: ObjectId("...") },
  { $set: { rating: 4.5, notes: "Интересная книга" } }
)
```

#### **3. Отметить книгу как прочитанную**

```javascript
db.books.updateOne(
  { _id: ObjectId("...") },
  { $set: { isRead: true } }
)
```

#### **4. Увеличить рейтинг**

```javascript
db.books.updateOne(
  { _id: ObjectId("...") },
  { $inc: { rating: 0.5 } }
)
```

#### **5. Массовое обновление**

```javascript
// Пометить все книги жанра "Классика" как важные
db.books.updateMany(
  { genre: "Классика" },
  { $set: { important: true } }
)

// Обнулить рейтинг всех непрочитанных книг
db.books.updateMany(
  { isRead: false },
  { $set: { rating: 0 } }
)
```

### **4.4. Удаление данных (DELETE)**

#### **1. Удалить книгу по ID**

```javascript
// DELETE /api/books/:id
const result = await booksCollection.findOneAndDelete({
  _id: new ObjectId(bookId)
});

if (!result.value) {
  throw new Error('Книга не найдена');
}

return result.value;
```

#### **2. Удалить все непрочитанные книги**

```javascript
db.books.deleteMany({ isRead: false })
```

#### **3. Удалить книги с нулевым рейтингом**

```javascript
db.books.deleteMany({ rating: 0 })
```

### **4.5. Агрегационные запросы (ANALYTICS)**

#### **1. Статистика библиотеки**

```javascript
// GET /api/stats
async function getStats() {
  const [totalBooks, readBooks, unreadBooks, avgRatingResult] = await Promise.all([
    booksCollection.countDocuments(),
    booksCollection.countDocuments({ isRead: true }),
    booksCollection.countDocuments({ isRead: false }),
    booksCollection.aggregate([
      { $match: { rating: { $gt: 0 } } },
      { $group: { _id: null, avgRating: { $avg: '$rating' } } }
    ]).toArray()
  ]);

  const averageRating = avgRatingResult.length > 0
    ? avgRatingResult[0].avgRating.toFixed(1)
    : 0;

  return {
    total: totalBooks,
    read: readBooks,
    unread: unreadBooks,
    averageRating
  };
}
```

**MongoDB команда:**

```javascript
// Общее количество книг
db.books.countDocuments()

// Количество прочитанных
db.books.countDocuments({ isRead: true })

// Средний рейтинг
db.books.aggregate([
  { $match: { rating: { $gt: 0 } } },
  { $group: { _id: null, avgRating: { $avg: '$rating' } } }
])
```

#### **2. Статистика по жанрам**

```javascript
db.books.aggregate([
  {
    $group: {
      _id: '$genre',
      count: { $sum: 1 },
      avgRating: { $avg: '$rating' },
      readCount: {
        $sum: { $cond: ['$isRead', 1, 0] }
      }
    }
  },
  {
    $project: {
      genre: '$_id',
      count: 1,
      avgRating: { $round: ['$avgRating', 2] },
      readCount: 1,
      readPercentage: {
        $multiply: [
          { $divide: ['$readCount', '$count'] },
          100
        ]
      }
    }
  },
  { $sort: { count: -1 } }
])
```

**Результат:**

```javascript
[
  {
    _id: "Фантастика",
    count: 15,
    avgRating: 4.5,
    readCount: 10,
    readPercentage: 66.67
  },
  {
    _id: "Классика",
    count: 12,
    avgRating: 4.8,
    readCount: 8,
    readPercentage: 66.67
  }
]
```

#### **3. Топ-10 книг по рейтингу**

```javascript
db.books.aggregate([
  { $match: { rating: { $gt: 0 } } },
  { $sort: { rating: -1, dateAdded: -1 } },
  { $limit: 10 },
  {
    $project: {
      title: 1,
      author: 1,
      rating: 1,
      genre: 1
    }
  }
])
```

#### **4. Книги по авторам**

```javascript
db.books.aggregate([
  {
    $group: {
      _id: '$author',
      bookCount: { $sum: 1 },
      avgRating: { $avg: '$rating' },
      books: { $push: '$title' }
    }
  },
  { $sort: { bookCount: -1 } },
  { $limit: 10 }
])
```

#### **5. Книги по годам**

```javascript
db.books.aggregate([
  { $match: { year: { $exists: true } } },
  {
    $group: {
      _id: '$year',
      count: { $sum: 1 },
      books: { $push: '$title' }
    }
  },
  { $sort: { _id: -1 } }
])
```

### **4.6. Сложные запросы**

#### **1. Пагинация**

```javascript
async function getBooksPaginated(page = 1, limit = 20) {
  const skip = (page - 1) * limit;
  
  const books = await booksCollection
    .find()
    .sort({ dateAdded: -1 })
    .skip(skip)
    .limit(limit)
    .toArray();
  
  const total = await booksCollection.countDocuments();
  const totalPages = Math.ceil(total / limit);
  
  return {
    books,
    pagination: {
      currentPage: page,
      totalPages,
      totalBooks: total,
      hasNextPage: page < totalPages,
      hasPrevPage: page > 1
    }
  };
}
```

#### **2. Полнотекстовый поиск**

```javascript
// Создание текстового индекса
db.books.createIndex({
  title: "text",
  author: "text",
  description: "text"
})

// Поиск по тексту
db.books.find({
  $text: { $search: "война толстой" }
})
```

#### **3. Книги за последний месяц**

```javascript
const oneMonthAgo = new Date();
oneMonthAgo.setMonth(oneMonthAgo.getMonth() - 1);

const recentBooks = await booksCollection
  .find({
    dateAdded: { $gte: oneMonthAgo }
  })
  .sort({ dateAdded: -1 })
  .toArray();
```

#### **4. Рекомендации на основе жанра**

```javascript
// Найти похожие книги (тот же жанр, исключая текущую)
async function getSimilarBooks(bookId, limit = 5) {
  const currentBook = await booksCollection.findOne({
    _id: new ObjectId(bookId)
  });
  
  if (!currentBook) return [];
  
  const similarBooks = await booksCollection
    .find({
      genre: currentBook.genre,
      _id: { $ne: new ObjectId(bookId) }
    })
    .sort({ rating: -1 })
    .limit(limit)
    .toArray();
  
  return similarBooks;
}
```

### **4.7. Примеры полных запросов из приложения**

```javascript
// Полный запрос с фильтрацией и сортировкой
app.get('/api/books', async (req, res) => {
  const { genre, isRead, sortBy } = req.query;
  const query = {};

  // Фильтрация
  if (genre && genre !== 'all') {
    query.genre = genre;
  }
  if (isRead !== undefined && isRead !== 'all') {
    query.isRead = isRead === 'true';
  }

  // Сортировка
  const sortOptions = {
    title: { title: 1 },
    author: { author: 1 },
    year: { year: -1 },
    rating: { rating: -1 },
    default: { dateAdded: -1 }
  };
  const sort = sortOptions[sortBy] || sortOptions.default;

  // Выполнение запроса
  const books = await booksCollection
    .find(query)
    .sort(sort)
    .toArray();

  res.json(books);
});
```

Данные запросы обеспечивают полный функционал работы с библиотекой книг: от базовых CRUD-операций до сложной аналитики и фильтрации.


## **5. Технологии доступа к БД**

Для взаимодействия с разработанной базой данных "Моя Библиотека", реализованной на основе MongoDB, используются различные технологии и инструменты, обеспечивающие прямой доступ к данным без использования ODM-библиотек. Технологии доступа включают нативные средства MongoDB и современные подходы к разработке веб-приложений.

### **5.1. MongoDB Native Driver для Node.js**

**MongoDB Node.js Driver** — официальный драйвер, предоставляющий прямой низкоуровневый доступ к MongoDB из приложений Node.js.

**Установка:**

```bash
npm install mongodb
```

**Основные возможности:**

- Прямые CRUD-операции без абстракций
- Полный контроль над запросами
- Поддержка всех возможностей MongoDB
- Высокая производительность
- Минимальные накладные расходы
- Асинхронные операции с Promise/async-await

**Пример подключения:**

```javascript
const { MongoClient, ObjectId } = require('mongodb');

const MONGODB_URI = 'mongodb://localhost:27017';
const client = new MongoClient(MONGODB_URI);

async function connectToMongoDB() {
  await client.connect();
  const db = client.db('bookLibrary');
  const booksCollection = db.collection('books');
  return { db, booksCollection };
}
```

**Преимущества нативного драйвера:**

✅ **Прямой контроль** - полный доступ к API MongoDB  
✅ **Производительность** - отсутствие промежуточных слоев  
✅ **Гибкость** - можно использовать любые возможности MongoDB  
✅ **Прозрачность** - видно что именно отправляется в БД  
✅ **Легковесность** - минимум зависимостей  

**Недостатки:**

❌ Нет автоматической валидации схемы  
❌ Необходимость ручной обработки данных  
❌ Больше boilerplate-кода  
❌ Требуется ручная работа с ObjectId  

### **5.2. Express.js для серверного API**

**Express.js** — минималистичный веб-фреймворк для Node.js, используемый для построения REST API.

**Установка:**

```bash
npm install express cors dotenv
```

**Архитектура приложения:**

```
Клиент (браузер) → HTTP → Express Server → MongoDB Driver → MongoDB
```

**Пример REST API:**

```javascript
const express = require('express');
const app = express();

// Middleware
app.use(express.json());
app.use(express.static('public'));

// API Routes
app.get('/api/books', async (req, res) => {
  const books = await booksCollection.find().toArray();
  res.json(books);
});

app.post('/api/books', async (req, res) => {
  const result = await booksCollection.insertOne(req.body);
  res.status(201).json(result);
});

app.listen(3000, () => {
  console.log('🚀 Сервер запущен на http://localhost:3000');
});
```

**Основные возможности:**

- Маршрутизация HTTP-запросов
- Middleware для обработки запросов
- Поддержка JSON
- Раздача статических файлов
- Обработка ошибок

### **5.3. MongoDB Shell (mongosh)**

**MongoDB Shell** — интерактивная консоль для работы с MongoDB через командную строку.

**Основные команды:**

```javascript
// Подключение
mongosh

// Переключение на БД
use bookLibrary

// Показать коллекции
show collections

// Просмотр данных
db.books.find().pretty()

// Подсчет документов
db.books.countDocuments()

// Создание индекса
db.books.createIndex({ title: 1 })

// Агрегация
db.books.aggregate([
  { $group: { _id: '$genre', count: { $sum: 1 } } }
])
```

**Используется для:**

- Первоначального наполнения БД
- Отладки запросов
- Администрирования
- Диагностики производительности
- Создания индексов
- Анализа данных

### **5.4. MongoDB Compass**

**MongoDB Compass** — графический интерфейс для работы с MongoDB.

**Функциональные особенности:**

- Визуальное отображение документов и коллекций
- Построение запросов через интерфейс
- Создание и управление индексами
- Анализ производительности запросов
- Просмотр плана выполнения запросов (explain)
- Экспорт/импорт данных

**Compass используется для:**

- Визуального анализа данных
- Тестирования запросов
- Мониторинга производительности
- Проверки структуры документов
- Отладки индексов

### **5.5. Fetch API для клиентского взаимодействия**

На стороне клиента используется **Fetch API** для взаимодействия с REST API сервера.

**Пример использования:**

```javascript
// Получение всех книг
async function loadBooks() {
  const response = await fetch('/api/books');
  const books = await response.json();
  return books;
}

// Создание книги
async function createBook(bookData) {
  const response = await fetch('/api/books', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(bookData)
  });
  return await response.json();
}

// Обновление книги
async function updateBook(id, bookData) {
  const response = await fetch(`/api/books/${id}`, {
    method: 'PUT',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(bookData)
  });
  return await response.json();
}

// Удаление книги
async function deleteBook(id) {
  await fetch(`/api/books/${id}`, {
    method: 'DELETE'
  });
}
```

### **5.6. MongoDB Atlas (облачное развертывание)**

**MongoDB Atlas** — облачная платформа для развертывания и управления MongoDB.

**Преимущества:**

- Автоматическое масштабирование
- Резервное копирование
- Мониторинг в реальном времени
- Глобальная репликация
- Безопасный доступ

**Подключение к Atlas:**

```javascript
const MONGODB_URI = 'mongodb+srv://username:password@cluster.mongodb.net/bookLibrary';
const client = new MongoClient(MONGODB_URI);
```

**Используется при:**

- Развертывании на продакшене
- Необходимости высокой доступности
- Работе с распределённой командой
- Требованиях к резервному копированию

### **5.7. Технологии безопасности**

**Механизмы безопасности MongoDB:**

1. **Аутентификация SCRAM** — безопасная проверка подлинности
2. **TLS/SSL шифрование** — защита данных при передаче
3. **IP Whitelist** — ограничение доступа по IP-адресам
4. **Role-Based Access Control** — управление правами доступа
5. **Журналы аудита** — отслеживание операций

**Пример безопасного подключения:**

```javascript
const client = new MongoClient(MONGODB_URI, {
  useNewUrlParser: true,
  useUnifiedTopology: true,
  ssl: true,
  authSource: 'admin'
});
```

### **5.8. Структура доступа к данным**

```
┌─────────────────┐
│   Браузер       │
│   (HTML/CSS/JS) │
└────────┬────────┘
         │ HTTP/AJAX
         ▼
┌─────────────────┐
│   Express.js    │
│   REST API      │
└────────┬────────┘
         │ MongoDB Driver
         ▼
┌─────────────────┐
│ MongoDB Native  │
│    Driver       │
└────────┬────────┘
         │ BSON Protocol
         ▼
┌─────────────────┐
│    MongoDB      │
│    Database     │
└─────────────────┘
```

### **5.9. Альтернативные драйверы и ORM**

Хотя в проекте используется нативный драйвер, существуют альтернативы:

**Mongoose (ODM):**
```javascript
const mongoose = require('mongoose');
// Предоставляет схемы и валидацию
```

**Prisma (ORM):**
```javascript
// TypeScript-first подход с генерацией типов
```

**TypeORM:**
```javascript
// ORM с поддержкой MongoDB
```

**Выбор нативного драйвера обоснован:**
- Прямой контроль над запросами
- Минимальные накладные расходы
- Лучшая производительность
- Гибкость в работе с данными
- Понимание работы MongoDB на низком уровне

### **5.10. Инструменты разработки**

**Используемые технологии:**

- **Node.js** v14+ — серверная платформа
- **MongoDB** v6.0+ — база данных
- **Express.js** v4.18+ — веб-фреймворк
- **MongoDB Driver** v6.3+ — драйвер для Node.js
- **dotenv** — управление переменными окружения
- **cors** — обработка CORS-запросов

**Команды для работы:**

```bash
# Установка зависимостей
npm install

# Запуск сервера (разработка)
npm run dev

# Запуск сервера (продакшн)
npm start

# Проверка подключения к MongoDB
mongosh --eval "db.version()"
```

Данный технологический стек обеспечивает прямое, эффективное и контролируемое взаимодействие с MongoDB, что критично для понимания работы NoSQL-баз данных и оптимизации производительности приложения.


## **Заключение**

В ходе выполнения проекта была разработана и реализована база данных на основе MongoDB для веб-приложения "Моя Библиотека", предназначенного для управления личной коллекцией книг. В процессе работы была проведена детальная проработка предметной области, определены сущности и их атрибуты, выполнена настройка валидации данных на уровне приложения, а также построена документно-ориентированная модель хранения.

Разработанная модель учитывает особенности функционирования системы управления библиотекой, обеспечивая эффективное хранение информации о книгах, включая библиографические данные, пользовательские оценки, заметки и статус прочтения. Применение документно-ориентированной структуры MongoDB позволило оптимизировать операции чтения и записи, исключив необходимость в сложных JOIN-запросах, характерных для реляционных баз данных.

Особенностью реализации является использование **нативного MongoDB драйвера** для Node.js вместо ODM-библиотек типа Mongoose. Такой подход обеспечивает:

- **Прямой контроль** над всеми операциями с базой данных
- **Высокую производительность** за счёт отсутствия промежуточных слоёв абстракции
- **Полное понимание** механизмов работы MongoDB
- **Гибкость** в реализации запросов и валидации данных
- **Минимальные накладные расходы** на обработку данных

В рамках проекта была создана коллекция books с полным набором индексов для оптимизации запросов, реализована валидация данных на уровне приложения, разработаны основные CRUD-операции и сложные агрегационные запросы для статистического анализа библиотеки. Дополнительно были рассмотрены современные технологии доступа к MongoDB, включая MongoDB Shell, Compass и облачную платформу Atlas.

Полученная база данных обладает следующими характеристиками:

✅ **Гибкость** — легко добавлять новые поля без миграций  
✅ **Производительность** — быстрый доступ к данным благодаря индексам  
✅ **Масштабируемость** — горизонтальное масштабирование через шардинг  
✅ **Простота** — понятная структура документов  
✅ **Эффективность** — отсутствие избыточных связей  

Реализованный REST API на базе Express.js предоставляет полный набор endpoints для взаимодействия с базой данных:

- `GET /api/books` — получение списка книг с фильтрацией и сортировкой
- `GET /api/books/:id` — получение информации о конкретной книге
- `POST /api/books` — добавление новой книги
- `PUT /api/books/:id` — обновление информации о книге
- `DELETE /api/books/:id` — удаление книги
- `GET /api/stats` — получение статистики библиотеки

Веб-интерфейс приложения обеспечивает удобное взаимодействие с системой через:

- **Панель статистики** с отображением общего количества книг, прочитанных и непрочитанных, среднего рейтинга
- **Систему фильтрации** по жанрам и статусу прочтения
- **Механизмы сортировки** по различным критериям (название, автор, год, рейтинг, дата добавления)
- **Модальные формы** для добавления и редактирования книг с валидацией
- **Интерактивный выбор рейтинга** в виде звёзд
- **Адаптивный дизайн** для работы на различных устройствах

Итоги работы подтверждают, что документно-ориентированная модель MongoDB в сочетании с нативным драйвером Node.js позволяет эффективно решать задачи, характерные для современных веб-приложений. Прямое взаимодействие с MongoDB без использования ODM-библиотек обеспечивает глубокое понимание принципов работы NoSQL-баз данных и даёт полный контроль над процессом обработки данных.

Проект может быть расширен за счёт:

- Реализации системы пользователей с аутентификацией
- Добавления функции обмена рецензиями между пользователями
- Интеграции с внешними API для автоматического получения информации о книгах
- Реализации рекомендательной системы на основе предпочтений
- Внедрения полнотекстового поиска с использованием MongoDB Atlas Search
- Добавления механизмов резервного копирования и репликации
- Реализации системы тегов и категорий для более гибкой организации

Таким образом, выполненная работа представляет собой полноценную основу для дальнейшего развития и внедрения в реальных условиях эксплуатации, демонстрируя практическое применение NoSQL-технологий и современных подходов к разработке веб-приложений.


## **Список использованных источников**

1. MongoDB. Официальная документация MongoDB [Электронный ресурс]. — Режим доступа: <https://www.mongodb.com/docs/> (дата обращения: 24.12.2024).

2. MongoDB Node.js Driver. Документация нативного драйвера [Электронный ресурс]. — Режим доступа: <https://www.mongodb.com/docs/drivers/node/current/> (дата обращения: 24.12.2024).

3. MongoDB Aggregation Framework. Руководство по агрегации [Электронный ресурс]. — Режим доступа: <https://www.mongodb.com/docs/manual/aggregation/> (дата обращения: 24.12.2024).

4. MongoDB Atlas. Руководство пользователя облачной платформы [Электронный ресурс]. — Режим доступа: <https://www.mongodb.com/atlas> (дата обращения: 24.12.2024).

5. Express.js. Официальная документация [Электронный ресурс]. — Режим доступа: <https://expressjs.com/> (дата обращения: 24.12.2024).

6. Node.js. Официальная документация [Электронный ресурс]. — Режим доступа: <https://nodejs.org/docs/> (дата обращения: 24.12.2024).

7. MongoDB Compass. Руководство пользователя [Электронный ресурс]. — Режим доступа: <https://www.mongodb.com/products/compass> (дата обращения: 24.12.2024).

8. MongoDB Indexes. Документация по индексам [Электронный ресурс]. — Режим доступа: <https://www.mongodb.com/docs/manual/indexes/> (дата обращения: 24.12.2024).

9. Элмасри Р., Наватэ Ш. Системы баз данных: фундаментальные принципы. — М.: Диалектика, 2020. — 864 с.

10. Брэдшоу Ш., Брейзил И., Ходоров К. MongoDB: The Definitive Guide. — O'Reilly Media, 2019. — 514 p.

